# Remove Unused CSS

![Remove Unused CSS](https://raw.githubusercontent.com/commeta/remove-unused-css/refs/heads/main/image.png "Remove Unused CSS")


Автоматический анализатор и оптимизатор CSS для удаления неиспользуемых селекторов с вашего веб-сайта. Инструмент помогает значительно уменьшить размер CSS файлов и улучшить производительность сайта.

## 🚀 Ключевые особенности

- **Автоматический анализ**: Сканирует все CSS файлы на странице в реальном времени
- **Интеллектуальное отслеживание**: Отслеживает использование селекторов при динамических изменениях DOM
- **Кросс-доменная поддержка**: Работает с локальными CSS файлами любого происхождения
- **Генерация оптимизированных файлов**: Создает очищенные версии CSS без неиспользуемых правил
- **Подробная статистика**: Показывает точную экономию байтов и количество удаленных селекторов
- **Удобный интерфейс**: Плавающая кнопка с интуитивным меню действий

## 📊 Преимущества для Google PageSpeed Insights

### Улучшение Core Web Vitals
- **Largest Contentful Paint (LCP)**: Уменьшение размера CSS файлов ускоряет загрузку критических ресурсов
- **First Input Delay (FID)**: Меньший объем CSS означает быстрее парсинг и меньше блокировки основного потока
- **Cumulative Layout Shift (CLS)**: Оптимизированный CSS загружается быстрее, предотвращая сдвиги макета

### Конкретные метрики производительности
- **Reduce unused CSS**: Прямое решение одной из главных рекомендаций PageSpeed Insights
- **Minimize main-thread work**: Уменьшение времени парсинга CSS
- **Reduce resource transfer sizes**: Экономия трафика за счет меньших файлов
- **Improve Time to Interactive (TTI)**: Быстрее время до интерактивности страницы

### Практические результаты
- Экономия от 30% до 80% размера CSS файлов в зависимости от проекта
- Улучшение скорости загрузки на 10-40% для CSS-тяжелых сайтов
- Повышение общего балла PageSpeed Insights на 5-20 пунктов

## 🔧 Принцип работы

### Клиентская часть (JavaScript)

**Этап 1: Инициализация и сбор данных**
Скрипт автоматически запускается при загрузке страницы и выполняет комплексный анализ:

- Сканирует все `<link rel="stylesheet">` элементы для получения списка CSS файлов
- Обнаруживает inline стили в `<style>` тегах
- Создает карту всех CSS файлов, присутствующих на текущей странице
- Фильтрует только локальные файлы (игнорирует внешние CDN)

**Этап 2: Парсинг CSS правил**
Система использует продвинутые техники для извлечения селекторов:

- Обращается к `document.styleSheets` API для получения CSS правил
- Обрабатывает кросс-доменные ограничения через повторную загрузку файлов
- Парсит сложные конструкции: `@media` правила, `@supports` блоки, `@import` директивы
- Нормализует селекторы и группирует их по исходным файлам
- Сохраняет контекст media queries для каждого селектора

**Этап 3: Интеллектуальное отслеживание использования**
Реализована система мониторинга в реальном времени:

- Периодически проверяет каждый селектор через `document.querySelector()`
- Отслеживает динамические изменения DOM с помощью `MutationObserver`
- Реагирует на изменения классов, ID и структуры элементов
- Помечает селекторы как "используемые" при первом обнаружении совпадения
- Ведет счетчик неиспользуемых селекторов в реальном времени

**Этап 4: Пользовательский интерфейс**
Интуитивная система управления:

- Плавающая кнопка показывает текущее количество неиспользуемых селекторов
- Выпадающее меню с действиями: сохранение данных и генерация файлов
- Визуальные индикаторы процесса обработки
- Подробные уведомления с результатами операций
- Статистика экономии места и производительности

### Серверная часть (PHP)

**Архитектура обработки**
Серверный компонент построен по принципу единой точки входа с маршрутизацией действий:

- Класс `RemoveUnusedCSSProcessor` инкапсулирует всю логику обработки
- Валидация входных данных и защита от некорректных запросов
- Обработка CORS заголовков для кросс-доменных запросов
- Централизованная система логирования ошибок

**Система накопления данных (Master Selectors)**
Уникальный подход к сбору статистики использования:

- Данные о селекторах накапливаются между сессиями в файле `unused_selectors.json`
- Селекторы получают статусы: "unused" (по умолчанию), "used" (обнаружен на странице)
- Система помнит все селекторы из файла, даже если они не встречались на текущей странице
- Интеллектуальное обновление статусов без потери накопленной информации

**Алгоритм обновления статусов**
Сложная логика определения использования селекторов:

- Загрузка существующих мастер-селекторов из JSON файла
- Инициализация новых файлов через парсинг с библиотекой `sabberworm/php-css-parser`
- Обновление статусов на основе данных от клиента
- Селектор становится "used" если найден хотя бы на одной странице
- Селектор остается "unused" только если не найден ни на одной проанализированной странице

**Генерация оптимизированных файлов**
Продвинутый алгоритм очистки CSS:

- Парсинг каждого CSS файла в Abstract Syntax Tree (AST)
- Рекурсивный обход всех типов правил: стили, media queries, supports блоки
- Удаление целых блоков деклараций если все селекторы неиспользуемые
- Сохранение правил если хотя бы один селектор используется  
- Очистка пустых `@media` блоков после удаления содержимого
- Создание индивидуальных очищенных файлов с сохранением структуры каталогов

**Система объединения и минификации**
Дополнительная оптимизация для продакшена:

- Генерация единого объединенного файла `remove-unused-css.min.css`
- Продвинутая минификация: удаление комментариев, нормализация пробелов
- Оптимизация синтаксиса: удаление лишних точек с запятой, пробелов вокруг операторов
- Создание карты файлов для отслеживания источников стилей

**Система безопасности**
Комплексные меры защиты:

- Валидация путей к файлам против path traversal атак
- Проверка принадлежности файлов к document root
- Ограничение размера обрабатываемых файлов (10MB)
- Фильтрация только локальных CSS файлов
- Защита от обработки внешних ресурсов

## 📈 Детальная статистика работы

Система предоставляет исчерпывающую аналитику процесса оптимизации:

**Метрики файлов:**
- Количество обработанных и сгенерированных файлов  
- Размер файлов до и после оптимизации
- Процент экономии дискового пространства
- Размер объединенного минифицированного файла

**Метрики селекторов:**
- Общее количество найденных селекторов
- Количество неиспользуемых селекторов по файлам
- Статистика удаленных правил
- Распределение по типам селекторов и media queries

## 🛠 Технические особенности

**Обработка сложных CSS конструкций:**
- Поддержка вложенных `@media` правил
- Корректная обработка `@supports` и `@import` директив
- Сохранение комментариев и форматирования в отладочном режиме
- Работа с псевдо-селекторами и комбинаторами

**Производительность и оптимизация:**
- Кеширование результатов парсинга CSS
- Батчевая обработка селекторов
- Минимизация количества DOM запросов
- Асинхронная обработка больших файлов

**Совместимость и надежность:**
- Работа со всеми современными браузерами
- Graceful degradation при ошибках парсинга
- Детальное логирование для диагностики проблем
- Автоматическое восстановление после сбоев

## 📋 Требования

- **PHP**: версия 7.4 или выше
- **Composer**: для установки зависимостей
- **sabberworm/php-css-parser**: библиотека для парсинга CSS
- **Веб-сервер**: с поддержкой PHP (Apache, Nginx)
- **Права доступа**: запись в директорию проекта для создания файлов

## 🎯 Результаты использования

После анализа вашего сайта инструмент предоставляет:

1. **Индивидуальные очищенные CSS файлы** - готовые к замене оригинальных файлов
2. **Объединенный минифицированный файл** - для максимальной производительности
3. **Подробный отчет** - с точными цифрами экономии и оптимизации
4. **Файл конфигурации** - для повторного анализа и обновления данных

Использование этого инструмента поможет вам достичь высоких показателей в Google PageSpeed Insights и существенно улучшить пользовательский опыт вашего веб-сайта за счет более быстрой загрузки страниц.





## Установка

### Требования

- PHP 7.4+ с поддержкой JSON
- Composer для установки зависимостей
- Веб-сервер с поддержкой PHP

### Установка через Composer

```bash
composer require sabberworm/php-css-parser
```

### Ручная установка

1. Скачайте файлы `remove-unused-css.php` и `remove-unused-css.js`
2. Поместите в корень вашего проекта или создайте отдельную директорию
3. Установите зависимость sabberworm/php-css-parser

## Подключение

### Подключение JavaScript

Добавьте скрипт перед закрывающим тегом `</body>`:

```html
<script src="/remove-unused-css/remove-unused-css.js"></script>
```

### Настройка конфигурации

В файле `remove-unused-css.js` можно изменить константы:

```javascript
const CONFIG = {
    CHECK_INTERVAL: 1000,           // Интервал проверки (мс)
    SERVER_ENDPOINT: '/remove-unused-css/remove-unused-css.php',
    BUTTON_ID: 'unused-css-button',
    MENU_ID: 'unused-css-menu'
};
```

### Права доступа

Убедитесь, что PHP-скрипт имеет права на:
- Чтение CSS-файлов вашего сайта
- Создание директории `data/` для сохранения данных
- Создание директории `css/` для выходных файлов

## Структура файлов после генерации

```
ваш-проект/
├── remove-unused-css/
│   ├── remove-unused-css.php      # Серверный обработчик
│   ├── remove-unused-css.js       # Клиентский скрипт
│   ├── data/
│   │   └── unused_selectors.json  # База данных селекторов
│   ├── css/                       # Очищенные CSS-файлы
│   │   ├── styles/
│   │   │   └── main.css          # Очищенная версия
│   │   ├── bootstrap/
│   │   │   └── bootstrap.min.css  # Очищенная версия
│   │   └── remove-unused-css.min.css  # Объединенный файл
│   └── error.log                  # Лог ошибок
```

### Файл данных unused_selectors.json

Содержит мастер-список всех селекторов:

```json
{
  "styles/main.css": {
    ".header": "used",
    ".sidebar": "unused",
    ".modal": "used"
  },
  "bootstrap/bootstrap.min.css": {
    ".container": "used",
    ".col-md-6": "used",
    ".badge-warning": "unused"
  }
}
```

## Использование

### 1. Сбор данных

**Важно**: Пройдите по всем страницам и состояниям интерфейса вашего сайта:

- Главная страница и все разделы
- Страницы товаров, категорий, статей
- Формы обратной связи и регистрации
- Модальные окна и всплывающие элементы
- Выпадающие меню и аккордеоны
- Мобильная версия и адаптивные состояния
- Состояния hover, focus, active
- Страницы ошибок (404, 500)

На каждой странице скрипт автоматически:
- Сканирует все подключенные CSS-файлы
- Проверяет наличие селекторов в DOM
- Отмечает используемые селекторы
- Накапливает данные в локальном хранилище

### 2. Генерация

После обхода всех страниц:

1. Нажмите на красную кнопку в правом нижнем углу экрана
2. В выпадающем меню выберите **«Сохранить данные»** для промежуточного сохранения
3. Выберите **«Генерировать файлы»** для создания очищенных версий

Система покажет подробную статистику:
- Количество обработанных файлов
- Размер экономии в байтах
- Количество удаленных селекторов
- Информацию об объединенном файле

### 3. Внедрение результатов

После генерации замените ссылки на CSS-файлы:

**Вместо оригинальных файлов:**
```html
<link rel="stylesheet" href="/styles/main.css">
<link rel="stylesheet" href="/bootstrap/bootstrap.min.css">
```

**Используйте очищенные версии:**
```html
<link rel="stylesheet" href="/remove-unused-css/css/styles/main.css">
<link rel="stylesheet" href="/remove-unused-css/css/bootstrap/bootstrap.min.css">
```

**Или объединенный файл:**
```html
<link rel="stylesheet" href="/remove-unused-css/css/remove-unused-css.min.css">
```

## JavaScript-константы

### Настраиваемые параметры

```javascript
// Интервал проверки селекторов (миллисекунды)
CHECK_INTERVAL: 1000

// Путь к серверному обработчику
SERVER_ENDPOINT: '/remove-unused-css/remove-unused-css.php'

// ID элементов интерфейса
BUTTON_ID: 'unused-css-button'
MENU_ID: 'unused-css-menu'
```

### Ограничения PHP

```php
// Максимальный размер обрабатываемого файла
MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB

// Пути к файлам данных
SELECTORS_FILE = 'data/unused_selectors.json'
CLEAN_CSS_DIR = 'css/'
COMBINED_CSS_FILE = 'remove-unused-css.min.css'
```

### Отладка и мониторинг

Для отладки используйте консоль браузера:

```javascript
// Доступ к состоянию приложения
console.log(window.unusedCSSState);

// Просмотр найденных селекторов
console.log(window.unusedCSSState.unusedSelectors);

// Информация о CSS-файлах
console.log(window.unusedCSSState.styleSheetsInfo);
```

## Рекомендации по использованию

### Периодичность обновления
- Запускайте анализ после каждого крупного обновления CSS
- Повторяйте процесс при добавлении новых страниц или компонентов
- Ведите версионность очищенных файлов

### Тестирование
- Тщательно протестируйте сайт после внедрения очищенных файлов
- Проверьте все интерактивные элементы и состояния
- Используйте инструменты автоматического тестирования интерфейса

### Резервное копирование
- Всегда сохраняйте оригинальные CSS-файлы
- Используйте систему контроля версий
- Документируйте изменения и их влияние на производительност


